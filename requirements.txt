# Núcleo (esqueleto/MVP)
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
psycopg[binary]>=3.1        # driver PostgreSQL (psycopg3)

# Auth — fatia de Identidade (passo 3)
python-jose[cryptography]>=3.3   # JWT
bcrypt>=4.0                      # hash de senha

# Testes
pytest>=8.0
httpx>=0.27                 # cliente de teste do FastAPI
