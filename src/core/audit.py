"""Trilha de auditoria (append-only).

Stub do esqueleto — obrigatório em Notas e Financeiro (ver CLAUDE.md §9):
registrar quem mudou o quê, quando e em qual tenant, na tabela `auditoria_log`.
"""

# TODO(auditoria): helper `registrar(acao, entidade, antes, depois, usuario, tenant)`
# gravando em auditoria_log de forma append-only.
