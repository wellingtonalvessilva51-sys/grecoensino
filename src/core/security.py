"""JWT, hashing e dependências de usuário atual.

Stub do esqueleto — implementação na fatia de Identidade (passo 3):
access token curto + refresh rotativo (hash, revogável), 403 estruturado.
"""

# TODO(identidade): criar/validar JWT, hash de senha (bcrypt/argon2),
# dependências `get_current_user` / `require_role`.
